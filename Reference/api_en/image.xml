<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>image()</name>

<category>Image</category>

<subcategory>Loading &amp; Displaying</subcategory>

<type></type>

<example>
<image />
<code><![CDATA[
img = None


def setup():
    # Images must be in the "data" directory to load correctly
    img = loadImage("laDefense.jpg")


def draw():
    image(img, 0, 0)
]]></code>
</example>


<example>
<image />
<code><![CDATA[
img = None


def setup():
    # Images must be in the "data" directory to load correctly
    img = loadImage("laDefense.jpg")


def draw():
    image(img, 0, 0)
    image(img, 0, 0, width / 2, height / 2)
]]></code>
</example>


<description><![CDATA[
The <b>image()</b> function draws an image to the display window. Images must be in the sketch's "data" directory to load correctly. Select "Add file..." from the "Sketch" menu to add the image to the data directory, or just drag the image file onto the sketch window. Processing currently works with GIF, JPEG, and PNG images. <br />
<br />
The <b>img</b> parameter specifies the image to display and by default the <b>a</b> and <b>b</b> parameters define the location of its upper-left corner. The image is displayed at its original size unless the <b>c</b> and <b>d</b> parameters specify a different size. The <b>imageMode()</b> function can be used to change the way these parameters draw the image.<br/>
<br/>
The color of an image may be modified with the <b>tint()</b> function. This function will maintain transparency for GIF and PNG images.
]]></description>

<syntax>
image(<c>img</c>, <c>a</c>, <c>b</c>)
image(<c>img</c>, <c>a</c>, <c>b</c>, <c>c</c>, <c>d</c>)
</syntax>

<parameter>
<label>img</label>
<description><![CDATA[PImage: the image to display]]></description>
</parameter>

<parameter>
<label>a</label>
<description><![CDATA[float: x-coordinate of the image]]></description>
</parameter>

<parameter>
<label>b</label>
<description><![CDATA[float: y-coordinate of the image]]></description>
</parameter>

<parameter>
<label>c</label>
<description><![CDATA[float: width to display the image]]></description>
</parameter>

<parameter>
<label>d</label>
<description><![CDATA[float: height to display the image]]></description>
</parameter>

<related>loadImage</related>
<related>PImage</related>
<related>imageMode</related>
<related>tint</related>
<related>background</related>
<related>alpha</related>

</root>
