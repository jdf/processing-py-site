<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>background()</name>

<category>Color</category>

<subcategory>Setting</subcategory>

<type></type>

<example>
<image />
<code><![CDATA[
background(51)
]]></code>
</example>

<example>
<image />
<code><![CDATA[
background(255, 204, 0)
]]></code>
</example>

<example>
<image />
<code><![CDATA[
img = loadImage("laDefense.jpg")
background(img)
]]></code>
</example>

<description><![CDATA[
The <b>background()</b> function sets the color used for the background of the Processing window. The default background is light gray. This function is typically used within <b>draw()</b> to clear the display window at the beginning of each frame, but it can be used inside <b>setup()</b> to set the background on the first frame of animation or if the backgound need only be set once.
<br/> <br/>
An image can also be used as the background for a sketch, although the image's width and height must match that of the sketch window. Images used with <b>background()</b> will ignore the current <b>tint()</b> setting. To resize an image to the size of the sketch window, use image.resize(width, height).
<br/> <br/>
It is not possible to use the transparency <b>alpha</b> parameter with background colors on the main drawing surface. It can only be used along with a <b>PGraphics</b> object and <b>createGraphics()</b>.
]]></description>

<syntax>
background(<c>rgb</c>)
background(<c>rgb</c>, <c>alpha</c>)
background(<c>gray</c>)
background(<c>gray</c>, <c>alpha</c>)
background(<c>v1</c>, <c>v2</c>, <c>v3</c>)
background(<c>v1</c>, <c>v2</c>, <c>v3</c>, <c>alpha</c>)
background(<c>image</c>)
</syntax>

<parameter>
<label>rgb</label>
<description><![CDATA[int: any value of the color datatype]]></description>
</parameter>

<parameter>
<label>alpha</label>
<description><![CDATA[float: opacity of the background]]></description>
</parameter>

<parameter>
<label>gray</label>
<description><![CDATA[float: specifies a value between white and black]]></description>
</parameter>

<parameter>
<label>v1</label>
<description><![CDATA[float: red or hue value (depending on the current color mode)]]></description>
</parameter>

<parameter>
<label>v2</label>
<description><![CDATA[float: green or saturation value (depending on the current color mode)]]></description>
</parameter>

<parameter>
<label>v3</label>
<description><![CDATA[float: blue or brightness value (depending on the current color mode)]]></description>
</parameter>

<parameter>
<label>image</label>
<description><![CDATA[PImage: PImage to set as background (must be same size as the sketch window)]]></description>
</parameter>


<related>stroke</related>
<related>fill</related>
<related>tint</related>
<related>colorMode</related>

</root>
