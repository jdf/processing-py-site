<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
<name>stroke()</name>

<category>Color</category>

<subcategory>Setting</subcategory>

<type></type>

<example>
<image />
<code><![CDATA[
stroke(153)
rect(30, 20, 55, 55)
]]></code>
</example>

<example>
<image />
<code><![CDATA[
stroke(204, 102, 0)
rect(30, 20, 55, 55)
]]></code>
</example>

<description><![CDATA[
Sets the color used to draw lines and borders around shapes. This color is either specified in terms of the RGB or HSB color depending on the current <b>colorMode()</b> (the default color space is RGB, with each value in the range from 0 to 255). 
<br/> <br/>
When using hexadecimal notation to specify a color, use "#" or "0x" before the values (e.g. #CCFFAA, 0xFFCCFFAA). The # syntax uses six digits to specify a color (the way colors are specified in HTML and CSS). When using the hexadecimal notation starting with "0x", the hexadecimal value must be specified with eight characters; the first two characters define the alpha component and the remainder the red, green, and blue components. 
<br/> <br/>
The value for the gray parameter must be less than or equal to the current maximum value as specified by <b>colorMode()</b>. The default maximum value is 255.
<br/> <br/>
When drawing in 2D with the default renderer, you may need <b>hint(ENABLE_STROKE_PURE)</b> to improve drawing quality (at the expense of performance). See the hint() documentation for more details.
]]></description>

<syntax>
	stroke(gs)
	stroke(r, g, b)
</syntax>

<parameter>
<label>gs</label>
<description><![CDATA[int: grayscale value for the stroke]]></description>
</parameter>

<parameter>
<label>r</label>
<description><![CDATA[int: red value for the stroke]]></description>
</parameter>

<parameter>
<label>g</label>
<description><![CDATA[int: green value for the stroke]]></description>
</parameter>

<parameter>
<label>b</label>
<description><![CDATA[int: blue value for the stroke]]></description>
</parameter>

<related>noStroke</related>
<related>strokeWeight</related>
<related>strokeJoin</related>
<related>strokeCap</related>
<related>fill</related>
<related>noFill</related>
<related>tint</related>
<related>background</related>
<related>colorMode</related>

</root>
